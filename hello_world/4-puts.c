#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
	{
		/* Using the function puts to print the desired message */
		puts("Programming is like building a multilingual puzzle");
		 /* Print a new line */
		 puts("");
		  /* End the program with the value 0 */
		  return (0);
	}
gcc -Wall -Werror -Wextra -pedantic -std=c89 4-puts.c -o 4-puts
./4-puts
  
